// Constants
// It keeps the defined value constant throughout the code.

let r = 5;
const pi = 3.14; // We are using const keyword here because we don't want to change the value of pi in the future.

let area

r = 6;
area = pi * r * r;

console.log(area);